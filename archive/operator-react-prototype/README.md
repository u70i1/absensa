# Archived operator scanner prototype

This React/Vite experiment predates the integrated FastAPI operator interface.
It is retained for reference only: its `/scan` request and independent frontend
workflow do not match the current authenticated application. The live operator
screens are in `web/app/templates/operator/` and use `web/app/static/` assets.
