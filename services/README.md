# Future services

This directory is reserved for services that run separately from the FastAPI
web app. The planned WhatsApp integration can be added here as its own Express.js
application using `whatsapp-web.js`, with its own `package.json`, configuration,
tests, and process lifecycle.

No WhatsApp service is implemented or started by the current project.
